/*
const readline = require("readline");

const rl = readline.createInterface(process.stdin,process.stdout);

rl.question("Ingresa tu nombre: ",function(res){
   console.log(`mi nombre es: ${res}`);
   rl.close();
});
*/


/*
const readline = require("readline");

const rl = readline.createInterface(process.stdin,process.stdout);

rl.setPrompt("Ingresa un mensaje (o 'fin' para salir): ");
rl.prompt();
rl.on("line", function(data){
   if(data === "fin"){
      rl.close();
      process.exit();
   }
   console.log(`Has escrito: ${data}`);
   rl.setPrompt("Ingresa un mensaje (o 'fin' para salir): ");
   rl.prompt();
});
*/



